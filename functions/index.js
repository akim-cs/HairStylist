const functions = require("firebase-functions");

// Placeholder for future server-side Cloud Functions.
// Email notifications are currently handled client-side via EmailJS.
exports.placeholder = functions.https.onRequest((req, res) => {
  res.send("OK");
});
